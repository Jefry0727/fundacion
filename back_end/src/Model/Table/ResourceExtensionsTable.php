<?php
namespace App\Model\Table;

use App\Model\Entity\ResourceExtension;
use Cake\ORM\Query;
use Cake\ORM\RulesChecker;
use Cake\ORM\Table;
use Cake\Validation\Validator;

/**
 * ResourceExtensions Model
 *
 * @property \Cake\ORM\Association\BelongsTo $ResourceFileTypes
 */
class ResourceExtensionsTable extends Table
{

    /**
     * Initialize method
     *
     * @param array $config The configuration for the Table.
     * @return void
     */
    public function initialize(array $config)
    {
        parent::initialize($config);

        $this->table('resource_extensions');
        $this->displayField('id');
        $this->primaryKey('id');

        $this->belongsTo('ResourceFileTypes', [
            'foreignKey' => 'resource_file_types_id',
            'joinType' => 'INNER'
        ]);
    }

    /**
     * Default validation rules.
     *
     * @param \Cake\Validation\Validator $validator Validator instance.
     * @return \Cake\Validation\Validator
     */
    public function validationDefault(Validator $validator)
    {
        $validator
            ->integer('id')
            ->allowEmpty('id', 'create');

        $validator
            ->requirePresence('extension', 'create')
            ->notEmpty('extension');

        return $validator;
    }

    /**
     * Returns a rules checker object that will be used for validating
     * application integrity.
     *
     * @param \Cake\ORM\RulesChecker $rules The rules object to be modified.
     * @return \Cake\ORM\RulesChecker
     */
    public function buildRules(RulesChecker $rules)
    {
        $rules->add($rules->existsIn(['resource_file_types_id'], 'ResourceFileTypes'));
        return $rules;
    }
}
